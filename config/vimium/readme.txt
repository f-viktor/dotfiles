this is a hacky fix for https://github.com/philc/vimium/issues/3452

1. Clone the vimium repo into a random folder
2. overwrite the two files with the ones in this folder
3. overwrite the custom CSS for Vimium UI for with the custom.css file
